\begin{figure}[t]
\centering
\begin{tikzpicture}[
    % Styles
    entity/.style={
        rectangle,
        draw=black,
        fill=#1,
        minimum width=1.8cm,
        minimum height=0.6cm,
        align=center,
        font=\footnotesize\bfseries
    },
    entity/.default=orange!20,
    lifeline/.style={
        dashed,
        gray
    },
    msg/.style={
        -Stealth,
        thick
    },
    msgback/.style={
        Stealth-,
        thick,
        dashed
    },
    note/.style={
        rectangle,
        draw=black!50,
        fill=yellow!15,
        font=\scriptsize,
        align=center,
        rounded corners=2pt,
        inner sep=3pt
    },
    loopbox/.style={
        rectangle,
        draw=black!60,
        rounded corners=2pt,
        inner sep=4pt
    }
]

% Entities (participants)
\node[entity] (provider) at (0, 0) {Provider};
\node[entity=blue!15] (dataroom) at (4.5, 0) {Data Room};
\node[entity=green!20] (memberA) at (9, 0) {Member A};
\node[entity=green!20] (memberB) at (12.5, 0) {Member B};

% Lifelines
\draw[lifeline] (provider.south) -- (0, -7);
\draw[lifeline] (dataroom.south) -- (4.5, -7);
\draw[lifeline] (memberA.south) -- (9, -7);
\draw[lifeline] (memberB.south) -- (12.5, -7);

% Step 1: Provider opens Data Room
\draw[msg] (0, -0.8) -- node[above, font=\scriptsize] {1. Open Data Room} (4.5, -0.8);

% Step 2: Provider invites members
\draw[msg] (0, -1.4) -- node[above, font=\scriptsize] {2. Invite members} (9, -1.4);
\draw[msg] (0, -1.4) -- (12.5, -1.4);

% Step 3: Assign roles
\draw[msg] (0, -2) -- node[above, font=\scriptsize] {3. Assign roles} (9, -2);
\draw[msg] (0, -2) -- node[below, font=\scriptsize] {(FM, tenant, investor, ...)} (12.5, -2);

% Loop box for requirements elicitation
\draw[loopbox] (-0.6, -2.6) rectangle (13.2, -4);
\node[font=\scriptsize\bfseries, anchor=north west] at (-0.5, -2.6) {loop};
\node[font=\scriptsize, anchor=north west, text=black!70] at (1, -2.6) {[for each role X]};

% Step 4: Elicit requirements
\draw[msg] (4.5, -3.2) -- node[above, font=\scriptsize] {4. ``What data do you want from role X?''} (9, -3.2);
\draw[msg] (4.5, -3.2) -- (12.5, -3.2);
\draw[msgback] (4.5, -3.8) -- node[above, font=\scriptsize] {5. Data requirements} (9, -3.8);
\draw[msgback] (4.5, -3.8) -- node[above, font=\scriptsize] {} (12.5, -3.8);

% Step 5: Publish collected requirements
\draw[msg] (4.5, -4.7) -- node[above, font=\scriptsize] {6. Publish all requirements} (9, -4.7);
\draw[msg] (4.5, -4.7) -- (12.5, -4.7);

% Step 6: Members review and self-select
\node[note] (evalA) at (9, -5.3) {Review\\requirements};
\node[note] (evalB) at (12.5, -5.3) {Review\\requirements};

% Step 7: Members identify partners and initiate contact
\draw[msg] (9, -6.1) -- node[above, font=\scriptsize] {7. Initiate contact} (12.5, -6.1);
\draw[msgback] (9, -6.7) -- node[above, font=\scriptsize] {8. Agree to share data} (12.5, -6.7);

% Annotations
% \draw[decorate, decoration={brace, amplitude=5pt, mirror}] (-1.0, -0.4) -- (-1.0, -2.5) node[midway, left=6pt, font=\scriptsize, align=right] {Setup};
% \draw[decorate, decoration={brace, amplitude=5pt}] (13.5, -2.6) -- (13.5, -4.7) node[midway, right=6pt, font=\scriptsize, align=left] {Requirements\\elicitation};
% \draw[decorate, decoration={brace, amplitude=5pt}] (13.5, -4.8) -- (13.5, -6.8) node[midway, right=6pt, font=\scriptsize, align=left] {Partner\\selection};

\end{tikzpicture}
\caption{Sequence diagram of the Data Room process: the Provider opens the Data Room, invites members, and assigns roles. Members are asked which data they would like to receive from each role. The collected requirements are published, enabling members to identify matching partners and initiate data sharing agreements.}
\label{fig:dataroom-process}
\end{figure}

A central concept in GRANERGIZE is the Data Room. Data Rooms are demarcated virtual areas with clearly defined access conditions, controlled by a \emph{Provider}. Only approved participants are permitted to enter, based on explicit authorization. Within a Data Room, stakeholders can negotiate and formalize data sharing agreements, ensuring that trust and consent are established before any data is exchanged.

We deliberately chose the term Data Room, as these spaces are conceptualized as protected and restricted areas. A Data Room is opened by a Provider -- typically a neutral party such as an industry association (e.g., Bundesvereinigung Logistik) or a project consortium -- who invites members and can remove them from the Data Room. Each member is assigned one or more roles reflecting their position in the logistics ecosystem, like FM, broker, tenant, investor, or benchmark service provider (BSP).
Then, the Provider asks each member which data they would like to receive from each role. This role-based elicitation produces a comprehensive map of data exchange pairs. The collected exchange pairs are then published to all participants, allowing participants to identify potential partners and to initiate contact.

% To illustrate this, consider the following scenario: The GRANERGIZE project team opens a Data Room, invites stakeholders from across the logistics real estate sector and assigns roles to each participant. Then we ask what data each participant would require from each role -- for example, what data a tenant wants from an FM, or what data an investor wants from a BSP. This role-based elicitation produces a comprehensive map of data exchange pairs. Collected exchange pairs are then published to all participants, allowing participants to identify potential partners and to initiate contact.
% \cref{fig:dataroom-process} illustrates this process.

In the logistics domain, energy consumption data of properties is relevant for a wide range of stakeholders. While users, owners, and FMs are primarily interested in specific consumption data for reporting and calculation purposes, developers, financiers, brokers, energy suppliers, and BSPs rely on data for property comparisons, accurate energy reporting, and consumption optimization.

To capture these interests and requirements, the project team opened a Data Room and invited stakeholders from across the logistics real estate sector.
\Cref{tbl:facroom} and \Cref{tbl:invroom} provide examples of the data exchange pairs that emerged from the elicitation process. In these tables, each row represents a data exchange where the party on the left acts as the \emph{Data Provider} and the party on the right as the \emph{Data Requester}. The arrow (\textrightarrow{}) indicates the direction of data flow from provider to requester.
\Cref{tbl:facroom} shows data exchange pairs involving the FM role, including exchanges with brokers (e.g., consumption data for energy performance certificates) and tenants (e.g., detailed consumption breakdowns including blind and peak loads). \Cref{tbl:invroom} illustrates data exchange pairs between an investor and a BSP, where the investor shares building specifics and consumption data, and the BSP computes and shares back aggregated consumption per~m\textsuperscript{2}.

\begingroup\renewcommand{\thefootnote}{\fnsymbol{footnote}}
\begin{table}
\caption{Overview of stakeholder data exchange pairs involving the facility manager (FM).}
\label{tbl:facroom} 
\begin{tabular}{p{2.4cm} p{5cm} p{4cm} p{2.7cm}}
\textbf{Data flow} & \textbf{Data points} & \textbf{Granularity} & \textbf{Periodicity} \\
\hline

FM \textrightarrow{} Broker 
& Consumption data (for energy performance certificate) 
& Consumption types$^*$ 
& annually \\

Broker \textrightarrow{} FM 
& User for new properties (as sales support) 
& Total property 
& once \\
\hline

FM \textrightarrow{} Tenant
& Consumption data 
& Consumption types$^*$, as granular as possible 
& At least monthly, ideally every 15 minutes \\

%FM \textrightarrow{} Tenant
& Blind loads
& Total property 
& once \\

%FM \textrightarrow{} Tenant
& Peak loads 
& Consumer types$^\dagger$
& As detailed as possible\\

Tenant \textrightarrow{} FM 
& Consumption data
& Total consumption per user 
& monthly \\

%Tenant \textrightarrow{} FM 
& Building specifics$^\ddagger$
& Number, size, location 
& annually \\
\hline
\end{tabular}
\smallskip
\noindent{\scriptsize $^*$Electricity, water, and gas.\quad $^\dagger$lighting, intralogistics, etc.\quad $^\ddagger$area, age, shift regime, etc.}
\end{table}

\begin{table}
\caption{Overview of stakeholder data exchange pairs between investor and benchmark service provider (BSP).
The BSP takes building specifics and consumption data and then provides the calculated consumption per m\textsuperscript{2}.
}
\label{tbl:invroom}
\begin{tabular}{p{2.4cm} p{5cm} p{4cm} p{2.7cm}}
\textbf{Data flow} & \textbf{Data points} & \textbf{Granularity} & \textbf{Periodicity} \\
\hline
Investor \textrightarrow{} BSP 
& Building specifics$^\ddagger$
& Total property 
& once \\

%Investor 
%BSP 
& Consumption data 
& Consumption types$^*$ 
& annually \\

%\rowcolor{gray!20}
BSP \textrightarrow{} Investor 

%& Min/max/median building specifics (area, age, shift regime\footnote{@@@shift regime could change over time, no? yes, might change, but is not very important; rather, the min/max/median building specifics is not shared back, but is needed to calculate the square meter aggregated value.}, etc.) 
%& Total property 
%& once \\

%BSP \textrightarrow{} Investor 
& Min/max/median consumption per m\textsuperscript{2} per property 
& Consumption types$^*$ 
& annually \\
\hline
\end{tabular}
\smallskip
\noindent{\scriptsize $^*$Electricity, water, and gas.\quad $^\ddagger$area, age, shift regime, etc.}
\end{table}
\endgroup

% This list can be supplemented by at least nine other identified pairs, each of which has expressed mutual data requirements in the context of energy consumption or property data for logistics real estate.
% In any case, it is necessary to exchange data beyond the boundaries of one's own company, which, due to the data silos that have been common up to now, different data formats and granularities, and general reservations about sharing data, is only possible with a great deal of individual effort.

% This is to be addressed and resolved through collaborative data sharing using the GRANERGIZE web app developed as part of the project.